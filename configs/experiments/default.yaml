# Default search limits for Lab experiments. Tasks may override.
search:
  strategy: progressive
  max_candidates: 24
  max_feature_group_combinations: 16
  max_hyperparameter_trials: 0
  max_ensemble_size: 7
  max_training_seconds: 60
  seed: 42
  exclude_high_leakage: true
