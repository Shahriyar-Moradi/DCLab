# Ensembles

Reuse of M1 helpers: greedy diversity selection (prediction correlation) and score-proportional blend.

The blend is the preferred result **only if** it beats the best single member on the validation primary metric. Reports always show baseline vs best single vs fusion.
